# Propiedades de servidor
server:
  port: 9050
  
# Configuraciones de eureka
eureka:
  client:
    service-url:
      defaultZone: http://172.18.4.96:8761/eureka

# Propiedad para que swagger resuelva correctamente los paths internos
spring:
  mvc:
    pathmatch:
      matching-strategy: ant-path-matcher